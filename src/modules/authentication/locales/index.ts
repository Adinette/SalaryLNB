import loginLocales from "../login/locales/index.ts";
import { type AuthenticationLocalization } from "../types/authentication_localization";

const frLocalization: AuthenticationLocalization = {
	login: loginLocales.fr,
};

export default {
	fr: frLocalization,
};
